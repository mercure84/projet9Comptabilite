sudo: required

language: java

services:
  - docker

before_install:
  - cd docker/dev/
  - docker-compose up

script:
  - mvn clean install -P test-business,test-consumer

after_script:
  - cd docker/dev
  - docker-compose stop
  - docker-compose rm -v -f